This Tracker allows you to track three different exercise types (cardio, flexibilty, and strength) and then comptues it all into one summary
The idea of it is to calculate all of the time and calories burned during the session
it uses both inheretance and comperhension in a number of different ways
it uses inheratance over the main exercise class and all of the subclasses around it so that we dont have to do as much code reuse but then demonstrates a high lovel of comprehension  when doing the demo file test.
it allows users to use different exersice namnes because we gave them the generic calss and they fill it using compreension
one challenge that I found to be pretty tough was just the implentaion across the different files and making them all talk to each other